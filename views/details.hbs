

<h1>{{cube.name}}</h1>
<img class="cube" src="{{cube.imageUrl}}" />
<div class="details">
  <p><span>Author:</span>{{cube.author}}</p>
  <p><span>Description:</span>{{cube.description}}</p>
  <p><span>Difficulty level:</span>{{cube.difficulty}}</p>
  <a class="btn" href="/products">Back</a>
  {{#if cube.isOwner}}
  <a class="btn" href="/products/details/{{cube._id}}/attach">Attach Stickers</a>
  <a class="btn" href="/products/edit/{{cube._id}}">EDIT</a>
  <a class="btn" href="/products/delete/{{cube._id}}">DELETE</a>
  {{/if}}
</div>
<h2>Accessories</h2>
            <div class="accessories">
               {{#each accessories}}
               {{> accessory}}
               {{else}}
                <h3 class="italic">This cube has no accessories yet...</h3>
               {{/each}}
            </div>
<div class="details">
  <form action="/comments/{{_id}}/create" method="POST">
    <span>Your name: </span><input type="text" name="author">
    <span>Comment:</span><br>
    <textarea name="content"></textarea>
    <input type="submit" value="Post Comment">
</form>
  {{#each comments}}
  {{> comment}}
  {{else}}
  <span style="font-style: italic;">No comments yet. Be the first!</span>
  {{/each}}

  
</div>
